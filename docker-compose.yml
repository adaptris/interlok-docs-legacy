version: '3'
services:
  jekyll:
    image: ricalo/jekyll
    container_name: jekyll
    hostname: jekyll
    ports:
      - '4000:4000'
    volumes:
      - .:/usr/src/app
    command: [ "server", "--config", "_config.yml,_config_dev.yml", "--force_polling" ]